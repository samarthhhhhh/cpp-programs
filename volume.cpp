#include <iostream>
#include <cmath>
using namespace std;

int main() {

	int side, volume;
	
	side = 3;
	volume = pow(side, 3);

	cout << "The volume of a cube with side = " << side << " is " << volume << endl;

	return 0;

}